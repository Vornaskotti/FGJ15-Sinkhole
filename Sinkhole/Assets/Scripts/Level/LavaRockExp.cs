using UnityEngine;
using System.Collections;

public class LavaRockExp : MonoBehaviour {

	// Use this for initialization
	void Start () {
		GameObject.Destroy(this, 5f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
